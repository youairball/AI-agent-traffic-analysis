# 04 请描述你使用 Agent 或 AI 驱动构建的具体成果

我基于多 Agent 架构重新开发了一个 AI 驱动的安全流量分析与自动化应急响应系统，项目名称为 **AI Agent SOC: Traffic Analysis and Incident Response**。

该项目面向企业安全运营 SOC 场景，重点解决传统安全分析中的四类问题：日志数据量大且格式不统一、固定规则检测误报率高、告警缺乏攻击链上下文、响应处置依赖人工判断。

系统采用多 Agent 协同架构，将安全分析流程拆分为独立 Agent，并通过消息总线进行解耦协作，形成从日志输入到告警响应的自动化闭环：

```text
日志输入
 -> LogParserAgent
 -> ThreatDetectionAgent
 -> AttackChainAgent
 -> ResponseAgent
 -> AlertManager
```

## 已实现的核心成果

1. **LogParserAgent 日志解析 Agent**

负责对原始安全日志进行结构化处理，从登录失败、异常访问、权限尝试等日志中提取时间、源 IP、事件类型、状态等关键字段，为后续 Agent 提供标准化输入。

2. **ThreatDetectionAgent 威胁检测 Agent**

结合规则推理与上下文状态累计识别威胁行为。例如系统会持续统计同一 IP 的登录失败次数，当短时间内多次失败达到阈值时，自动触发暴力破解告警。相比传统单条日志规则，该方式引入了历史上下文，可有效减少孤立事件带来的误报。

3. **AttackChainAgent 攻击链分析 Agent**

该 Agent 会将多个告警按时间序列和行为上下文进行关联，自动生成攻击链视角。例如：

```text
暴力破解 -> 凭证获取 -> 权限尝试 -> 横向移动
```

这使系统从“单点告警检测”升级为“多阶段行为链分析”，更接近真实安全运营中的研判流程。

4. **ResponseAgent 响应生成 Agent**

系统会根据攻击类型自动生成处置建议，例如封禁攻击 IP、启用 MFA、多阶段日志审计、检查系统登录日志和权限变更记录等。当前版本支持规则与模板生成，后续可接入 MiMo、DeepSeek、Qwen 等模型进行更复杂的长链推理和响应策略生成。

5. **AlertManager 告警管理 Agent**

负责统一输出告警、保存处理状态，并向可视化页面导出实时数据。项目中已经实现终端运行日志和 Agent 工作流可视化页面，方便展示系统的实际运行效果。

## 多 Agent 协作机制

系统没有采用模块之间直接调用的方式，而是使用：

```text
Agent -> Message Bus -> Agent
```

各 Agent 通过消息总线订阅和发布事件。这种方式带来三个优势：

- 低耦合：每个 Agent 只关注自己的输入和输出
- 易扩展：后续可以新增 WebTrafficAgent、MalwareAgent、MITREMappingAgent
- 易并行：不同检测和分析 Agent 可以独立运行，提高处理效率

## 推理机制

项目当前包含三类推理方式：

1. **规则驱动推理**

例如连续失败登录次数超过阈值时判定为 Brute Force。该方式稳定、可解释，适合高频安全事件检测。

2. **上下文关联推理**

系统会跨多条日志维护同一 IP、同一攻击阶段的上下文状态，将孤立日志还原为连续攻击行为。

3. **AI 长链推理扩展**

项目架构已经预留模型接入位置，后续可将多条日志、告警、攻击链上下文输入 MiMo 等大模型，由模型生成更完整的攻击路径解释、风险等级判断和响应方案。

## 当前已完成能力

- 多 Agent 工作流
- 安全日志自动解析
- 实时威胁检测
- 上下文状态累计
- 暴力破解告警识别
- 攻击链自动构建
- 响应建议自动生成
- 终端运行日志输出
- Agent 工作流可视化页面
- 单元测试验证

## 使用证明与影响力

我已在过去 30 天持续使用多个主流大模型平台进行 Agent 推理、安全分析和自动化工作流实验，包括 OpenAI、Google AI Studio 和 Anthropic Claude。

过去 30 天真实 API 使用费用如下：

- OpenAI: 56.21 USD
- Google AI Studio: 18.36 USD
- Anthropic Claude: 32.41 USD

这些使用主要用于多 Agent 协同实验、AI 长链推理测试、攻击链推断、响应策略生成和安全自动化分析验证。

本项目后续计划进一步接入 Xiaomi MiMo API，用于构建面向智能 SOC 的大模型驱动安全分析平台，包括：

- 复杂日志上下文理解
- 攻击路径自动解释
- MITRE ATT&CK 技术映射
- 自动响应策略生成
- 安全事件复盘报告生成

该项目不是单纯的文本描述，而是包含可运行代码、终端日志输出、可视化工作流页面和测试用例的完整 Agent 项目，可用于真实展示 AI Agent 在安全运营场景中的落地能力。
